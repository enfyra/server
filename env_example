#DB SETTING
DB_TYPE=mysql #mysql | mariadb | postgres
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=1234
DB_NAME=enfyra

#DATABASE CONNECTION POOL SETTINGS
DB_POOL_SIZE=100                    # Max connections in pool (all databases)
DB_CONNECTION_LIMIT=100             # MySQL/MariaDB connection limit, PostgreSQL max connections
DB_ACQUIRE_TIMEOUT=60000            # Connection acquisition timeout (ms)
DB_IDLE_TIMEOUT=30000               # PostgreSQL idle connection timeout (ms)

#REDIS SETTING
REDIS_URI=redis://localhost:6379
DEFAULT_TTL=5

#APP SETTING
NODE_NAME=my_enfyra
PORT=1105
DEFAULT_HANDLER_TIMEOUT=5000
DEFAULT_PREHOOK_TIMEOUT=3000
DEFAULT_AFTERHOOK_TIMEOUT=3000

#AUTH_SETTING
SECRET_KEY=my_secret
SALT_ROUNDS=10
ACCESS_TOKEN_EXP=15m
REFRESH_TOKEN_NO_REMEMBER_EXP=1d
REFRESH_TOKEN_REMEMBER_EXP=7d

PACKAGE_MANAGER=yarn